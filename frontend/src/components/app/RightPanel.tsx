// This might potentially be redundant since the <Outlet> in __layout.tsx will act as the right panel in my app. 
// But if I'm planning on having a wrapper around <Outlet> to add padding or styling then I should keep this.
// Otherwise safe to delete.